#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int digito;
    vector<int> D1;
    while(cin >> digito)
    {
        D1.insert(D1.begin(), digito);
    }

    int acarreado = 1;
    while (acarreado > 0 && )
    {

    }
    return 0;
}
